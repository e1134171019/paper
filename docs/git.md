# Git guide

- `main` is the stable baseline.
- Feature work is developed on a dedicated branch and reviewed before merge.
- Do not commit `.env`, local SQLite databases, caches, test output, or downloaded publisher PDFs.
- Commit generated CSV only when it is intentionally reviewed as a human-readable research snapshot.
- Keep design/plan documents in `docs/superpowers/` so implementation decisions remain traceable.
